{
  "name": "gogabanda-server",
  "version": "1.0.0",
  "description": "Backend per il sistema buzzer",
  "main": "server.cjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.cjs",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "path": "^0.12.7"
  }
}